---
"@adobe/spectrum-design-data-mcp": minor
---

Fix token data loading to use `getTokensByFile`/`getAllTokens` from `@adobe/spectrum-tokens` (works in monorepo and via npx). Add `query-tokens-by-value` tool to search tokens by direct or resolved (alias) value.
