# Conformance fixtures

Layer 3 test data. Structure and cases are tracked in [#734](https://github.com/adobe/spectrum-design-data/issues/734).

* `valid/` — documents that must pass validation
* `invalid/SPEC-NNN/` — negative tests with `expected-errors.json` per rule
