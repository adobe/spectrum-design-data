[package]
name = "design-data-shadcn"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "shadcn registry (registry:theme) export for design-data — validation spike"

[lib]
path = "src/lib.rs"

[dependencies]
design-data-core = { path = "../../core" }
design-data-dtcg = { path = "../dtcg" }
serde_json = { version = "1.0", features = ["preserve_order"] }
