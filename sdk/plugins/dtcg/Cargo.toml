[package]
name = "design-data-dtcg"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "W3C DTCG token document export for design-data — not owned/maintained by Adobe"

[lib]
path = "src/lib.rs"

[dependencies]
design-data-core = { path = "../../core" }
serde_json = { version = "1.0", features = ["preserve_order"] }
