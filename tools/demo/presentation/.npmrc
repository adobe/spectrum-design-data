# Slidev pulls napi-rs packages (oxc-parser, etc.) whose native .node bindings
# don't always resolve under pnpm's default isolated node_modules. Hoisting the
# node_modules layout makes the platform binding discoverable. This file applies
# only to this standalone deck (installed with --ignore-workspace).
node-linker=hoisted
