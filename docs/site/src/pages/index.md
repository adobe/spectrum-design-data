***

title: Home
layout: base.liquid
permalink: /
------------

# Spectrum Design Data

API documentation, design tokens, and registry for **Spectrum**, Adobe's design system.

## Sections

* **\[Components]\({{ pathPrefix }}/components/)** — Component API schemas (properties, types, defaults)
* **\[Tokens]\({{ pathPrefix }}/tokens/)** — Design tokens (color, typography, layout, etc.)
* **\[Registry]\({{ pathPrefix }}/registry/)** — Design system terminology (sizes, states, variants, glossary)

## Tools

* \[S2 Tokens Viewer]\({{ pathPrefix }}/s2-tokens-viewer/) — Browse Spectrum 2 tokens
* \[S2 Visualizer]\({{ pathPrefix }}/s2-visualizer/) — Token dependency graph (Spectrum 2)
* \[Visualizer]\({{ pathPrefix }}/visualizer/) — Token dependency graph (Spectrum 1)
* \[Release Timeline]\({{ pathPrefix }}/release-timeline/) — Release history
